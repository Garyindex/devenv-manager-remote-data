{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/GaryIndex/devenv-manager-remote-data/main/schemas/install-versions.schema.json",
  "title": "DevEnv Manager online tool metadata",
  "type": "object",
  "required": ["schemaVersion", "generatedAt", "status", "summary", "tools", "errors"],
  "properties": {
    "schemaVersion": { "type": "integer", "minimum": 1 },
    "generatedAt": { "type": "string" },
    "purpose": { "type": "string" },
    "platform": { "type": "string" },
    "status": { "type": "string" },
    "summary": { "type": "object" },
    "tools": { "type": "array", "items": { "$ref": "#/$defs/toolMetadata" } },
    "errors": { "type": "array", "items": { "type": "string" } }
  },
  "$defs": {
    "toolMetadata": {
      "type": "object",
      "required": ["id", "name", "categoryId", "platforms", "descriptions", "usage", "notes", "detection", "verify", "quality", "sources"],
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "categoryId": { "type": "string" },
        "platforms": { "type": "array", "items": { "type": "string" } },
        "descriptions": { "$ref": "#/$defs/descriptions" },
        "usage": { "$ref": "#/$defs/usage" },
        "notes": { "$ref": "#/$defs/notes" },
        "detection": { "type": "object" },
        "verify": { "$ref": "#/$defs/verify" },
        "dependencies": { "type": "object" },
        "risk": { "type": "object" },
        "quality": { "$ref": "#/$defs/quality" },
        "sources": { "type": "array", "items": { "$ref": "#/$defs/sourceMetadata" } }
      }
    },
    "sourceMetadata": {
      "type": "object",
      "required": ["id", "manager", "packageId", "platforms", "scan", "package", "links", "descriptions", "usage", "notes", "versions", "downloads", "commands", "quality", "verify"],
      "properties": {
        "id": { "type": "string" },
        "manager": { "type": "string" },
        "packageId": { "type": ["string", "null"] },
        "platforms": { "type": "array", "items": { "type": "string" } },
        "scan": { "type": "object" },
        "package": { "type": "object" },
        "links": { "type": "object" },
        "descriptions": { "$ref": "#/$defs/descriptions" },
        "usage": { "$ref": "#/$defs/usage" },
        "notes": { "$ref": "#/$defs/notes" },
        "versions": { "type": "array", "items": { "$ref": "#/$defs/version" } },
        "downloads": { "type": "array", "items": { "$ref": "#/$defs/download" } },
        "commands": { "type": "array", "items": { "$ref": "#/$defs/command" } },
        "quality": { "$ref": "#/$defs/quality" },
        "verify": { "$ref": "#/$defs/verify" }
      }
    },
    "descriptions": {
      "type": "object",
      "required": ["short", "long", "source", "homepage", "lastUpdatedAt"],
      "properties": {
        "short": { "type": ["string", "null"], "maxLength": 280 },
        "long": { "type": ["string", "null"], "maxLength": 1200 },
        "source": { "type": "string" },
        "homepage": { "type": ["string", "null"] },
        "lastUpdatedAt": { "type": ["string", "null"] }
      }
    },
    "usage": {
      "type": "object",
      "required": ["primaryUseCases", "keywords", "relatedTools"],
      "properties": {
        "primaryUseCases": { "type": "array", "items": { "type": "string" } },
        "keywords": { "type": "array", "items": { "type": "string" } },
        "relatedTools": { "type": "array", "items": { "type": "string" } }
      }
    },
    "notes": {
      "type": "object",
      "required": ["install", "upgrade", "knownIssues"],
      "properties": {
        "install": { "type": "array", "items": { "type": "string" } },
        "upgrade": { "type": "array", "items": { "type": "string" } },
        "knownIssues": { "type": "array", "items": { "type": "string" } }
      }
    },
    "verify": {
      "type": "object",
      "required": ["commands"],
      "properties": {
        "commands": {
          "type": "array",
          "items": {
            "type": "object",
            "required": ["command", "args", "expectedPattern"],
            "properties": {
              "command": { "type": "string" },
              "args": { "type": "array", "items": { "type": "string" } },
              "expectedPattern": { "type": "string" }
            }
          }
        }
      }
    },
    "quality": {
      "type": "object",
      "required": ["confidence", "score"],
      "properties": {
        "confidence": { "type": "string", "enum": ["high", "medium", "low"] },
        "score": { "type": "number", "minimum": 0, "maximum": 100 },
        "official": { "type": "boolean" },
        "lastSuccessfulScanAt": { "type": ["string", "null"] },
        "failureCount": { "type": "integer", "minimum": 0 },
        "staleAfterDays": { "type": "integer", "minimum": 1 }
      }
    },
    "version": {
      "type": "object",
      "required": ["version", "channel", "latest", "prerelease"],
      "properties": {
        "version": { "type": "string" },
        "channel": { "type": "string" },
        "latest": { "type": "boolean" },
        "prerelease": { "type": "boolean" },
        "lts": { "type": ["boolean", "null"] },
        "releaseDate": { "type": ["string", "null"] },
        "eolDate": { "type": ["string", "null"] },
        "changelogUrl": { "type": ["string", "null"] }
      }
    },
    "download": {
      "type": "object",
      "required": ["id", "platform", "urlType", "direct"],
      "properties": {
        "id": { "type": "string" },
        "version": { "type": ["string", "null"] },
        "platform": { "type": "string" },
        "architecture": { "type": ["string", "null"] },
        "type": { "type": "string" },
        "url": { "type": ["string", "null"] },
        "urlType": { "type": "string" },
        "direct": { "type": "boolean" },
        "sha256": { "type": ["string", "null"] },
        "sizeBytes": { "type": ["number", "null"] }
      }
    },
    "command": {
      "type": "object",
      "required": ["action", "manager", "platform", "requiresAdmin", "supportsVersion", "shell", "command", "template"],
      "properties": {
        "action": { "type": "string" },
        "manager": { "type": "string" },
        "platform": { "type": "string" },
        "version": { "type": ["string", "null"] },
        "requiresAdmin": { "type": "boolean" },
        "supportsVersion": { "type": "boolean" },
        "shell": { "type": "string" },
        "command": { "type": "array", "items": { "type": "string" } },
        "template": { "type": "array", "items": { "type": "string" } }
      }
    }
  }
}
